# AI Video Watcher - Requirements
# Core Streamlit and video processing
streamlit>=1.28.0
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0

# OpenAI integration (using API, not local models)
openai>=1.0.0
httpx

# Database
psycopg2-binary

# Report generation
reportlab

# Memory monitoring
psutil

# Note: FFmpeg system binary provided by Nix packages in .replit
# All audio transcription and speaker diarization uses OpenAI APIs (Whisper-1, GPT-5 mini)
# No local ML models (PyTorch, TensorFlow) required